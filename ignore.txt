# ignore.txt — skill names that pull and push must never sync.
#
# One skill name per line (must match the directory name under skills/).
# Blank lines and lines starting with # are ignored.
#
# pull will not pull these in from ~/.claude/skills.
# push will not push these out to ~/.claude/skills.

eli5
graphify
